services:
  app:
    container_name: php-symfony-filename-parser-app
    build:
      context: ..
      dockerfile: docker/Dockerfile
    working_dir: /app
    volumes:
      - ../:/app
    tty: true
